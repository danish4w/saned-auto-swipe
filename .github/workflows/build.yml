name: Build Saned Auto Swipe
on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build Project
        run: |
          mkdir -p app/src/main/java/com/saned/autoswipe
          mkdir -p app/src/main/res/layout
          mkdir -p app/src/main/res/xml
          mkdir -p app/src/main/res/values
          mkdir -p app/src/main/res/mipmap-xxxhdpi
          mkdir -p gradle/wrapper

          curl -fsSL https://raw.githubusercontent.com/gradle/gradle/v8.9.0/gradlew -o gradlew
          curl -fsSL https://raw.githubusercontent.com/gradle/gradle/v8.9.0/gradle/wrapper/gradle-wrapper.jar -o gradle/wrapper/gradle-wrapper.jar
          curl -fsSL https://raw.githubusercontent.com/gradle/gradle/v8.9.0/gradle/wrapper/gradle-wrapper.properties -o gradle/wrapper/gradle-wrapper.properties
          chmod +x gradlew

          echo "include ':app'" > settings.gradle

          cat > gradle.properties << 'EOF'
          android.useAndroidX=true
          android.enableJetifier=false
          org.gradle.jvmargs=-Xmx2048m
          EOF

          cat > build.gradle << 'EOF'
          buildscript {
              repositories { google(); mavenCentral() }
              dependencies { classpath 'com.android.tools.build:gradle:8.3.2' }
          }
          allprojects {
              repositories { google(); mavenCentral() }
          }
          EOF

          cat > app/build.gradle << 'EOF'
          plugins { id 'com.android.application' }
          android {
              namespace 'com.saned.autoswipe'
              compileSdk 34
              defaultConfig {
                  applicationId 'com.saned.autoswipe'
                  minSdk 26
                  targetSdk 34
                  versionCode 5
                  versionName '5.0'
              }
              packagingOptions {
                  exclude 'META-INF/LICENSE.md'
                  exclude 'META-INF/LICENSE-notice.md'
              }
          }
          dependencies {
              implementation 'androidx.appcompat:appcompat:1.6.1'
              implementation 'androidx.core:core:1.10.1'
          }
          EOF

          python3 - << 'PYEOF'
          import base64
          b64 = ""
          import os
          data = base64.b64decode(b64 + "==")
          os.makedirs('app/src/main/res/mipmap-xxxhdpi', exist_ok=True)
          PYEOF

          python3 - << 'PYEOF'
          import base64, os
          b64 = ""
          data = base64.b64decode(b64)
          with open('app/src/main/res/mipmap-xxxhdpi/ic_launcher.png', 'wb') as f:
              f.write(data)
          print("Icon OK")
          PYEOF

          cat > app/src/main/AndroidManifest.xml << 'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
              <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
              <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
              <application
                  android:label="Saned Auto"
                  android:icon="@mipmap/ic_launcher"
                  android:theme="@style/Theme.AppCompat">
                  <activity
                      android:name=".MainActivity"
                      android:exported="true">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN"/>
                          <category android:name="android.intent.category.LAUNCHER"/>
                      </intent-filter>
                  </activity>
                  <service
                      android:name=".AutoSwipeService"
                      android:exported="true"
                      android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
                      <intent-filter>
                          <action android:name="android.accessibilityservice.AccessibilityService"/>
                      </intent-filter>
                      <meta-data
                          android:name="android.accessibilityservice"
                          android:resource="@xml/acc_config"/>
                  </service>
              </application>
          </manifest>
          EOF

          cat > app/src/main/res/xml/acc_config.xml << 'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <accessibility-service
              xmlns:android="http://schemas.android.com/apk/res/android"
              android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
              android:accessibilityFeedbackType="feedbackGeneric"
              android:accessibilityFlags="flagDefault|flagReportViewIds|flagRetrieveInteractiveWindows"
              android:canPerformGestures="true"
              android:canRetrieveWindowContent="true"
              android:notificationTimeout="100"
              android:packageNames="com.saned.delivery"/>
          EOF

          cat > app/src/main/res/values/strings.xml << 'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <resources>
              <string name="app_name">Saned Auto</string>
              <string name="channel_id">saned_auto_channel</string>
              <string name="channel_name">Saned Auto Notifications</string>
          </resources>
          EOF

          cat > app/src/main/res/layout/activity_main.xml << 'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="#121212">
              <LinearLayout
                  android:layout_width="match_parent"
                  android:layout_height="wrap_content"
                  android:orientation="vertical"
                  android:gravity="center_horizontal"
                  android:padding="24dp">

                  <TextView
                      android:layout_width="wrap_content"
                      android:layout_height="wrap_content"
                      android:text="Saned Auto Swipe"
                      android:textSize="24sp"
                      android:textColor="#FFFFFF"
                      android:textStyle="bold"
                      android:layout_marginBottom="6dp"/>

                  <TextView
                      android:id="@+id/tv_status"
                      android:layout_width="wrap_content"
                      android:layout_height="wrap_content"
                      android:text="Service OFF"
                      android:textSize="15sp"
                      android:textColor="#FF5555"
                      android:layout_marginBottom="28dp"/>

                  <!-- PICKUP MAX ONLY -->
                  <TextView
                      android:layout_width="match_parent"
                      android:layout_height="wrap_content"
                      android:text="Pickup Distance (From You)"
                      android:textColor="#1DB954"
                      android:textSize="15sp"
                      android:textStyle="bold"
                      android:layout_marginBottom="8dp"/>

                  <TextView
                      android:layout_width="match_parent"
                      android:layout_height="wrap_content"
                      android:text="Max (Km)"
                      android:textColor="#AAAAAA"
                      android:textSize="13sp"
                      android:gravity="center"/>
                  <EditText
                      android:id="@+id/et_pickup_max"
                      android:layout_width="200dp"
                      android:layout_height="wrap_content"
                      android:inputType="numberDecimal"
                      android:text="5.0"
                      android:textColor="#FFFFFF"
                      android:backgroundTint="#333333"
                      android:gravity="center"
                      android:padding="10dp"
                      android:layout_marginBottom="4dp"/>
                  <TextView
                      android:layout_width="match_parent"
                      android:layout_height="wrap_content"
                      android:text="(0 se is tak ke orders accept honge)"
                      android:textColor="#777777"
                      android:textSize="11sp"
                      android:gravity="center"
                      android:layout_marginBottom="16dp"/>

                  <View
                      android:layout_width="match_parent"
                      android:layout_height="1dp"
                      android:background="#333333"
                      android:layout_marginVertical="8dp"/>

                  <!-- DROP MAX ONLY -->
                  <TextView
                      android:layout_width="match_parent"
                      android:layout_height="wrap_content"
                      android:text="Drop Distance (From Pickup)"
                      android:textColor="#FF9500"
                      android:textSize="15sp"
                      android:textStyle="bold"
                      android:layout_marginBottom="8dp"
                      android:layout_marginTop="8dp"/>

                  <TextView
                      android:layout_width="match_parent"
                      android:layout_height="wrap_content"
                      android:text="Max (Km)"
                      android:textColor="#AAAAAA"
                      android:textSize="13sp"
                      android:gravity="center"/>
                  <EditText
                      android:id="@+id/et_drop_max"
                      android:layout_width="200dp"
                      android:layout_height="wrap_content"
                      android:inputType="numberDecimal"
                      android:text="10.0"
                      android:textColor="#FFFFFF"
                      android:backgroundTint="#333333"
                      android:gravity="center"
                      android:padding="10dp"
                      android:layout_marginBottom="4dp"/>
                  <TextView
                      android:layout_width="match_parent"
                      android:layout_height="wrap_content"
                      android:text="(0 se is tak ke orders accept honge)"
                      android:textColor="#777777"
                      android:textSize="11sp"
                      android:gravity="center"
                      android:layout_marginBottom="16dp"/>

                  <View
                      android:layout_width="match_parent"
                      android:layout_height="1dp"
                      android:background="#333333"
                      android:layout_marginVertical="8dp"/>

                  <Switch
                      android:id="@+id/switch_service"
                      android:layout_width="wrap_content"
                      android:layout_height="wrap_content"
                      android:text="Auto Accept"
                      android:textColor="#FFFFFF"
                      android:textSize="18sp"
                      android:layout_marginTop="12dp"
                      android:layout_marginBottom="16dp"/>

                  <Button
                      android:id="@+id/btn_save"
                      android:layout_width="match_parent"
                      android:layout_height="wrap_content"
                      android:text="OK - Save Settings"
                      android:textSize="16sp"
                      android:textStyle="bold"
                      android:backgroundTint="#1DB954"
                      android:layout_marginBottom="16dp"/>

                  <TextView
                      android:id="@+id/tv_info"
                      android:layout_width="wrap_content"
                      android:layout_height="wrap_content"
                      android:text="Pehle Accessibility Service enable karein"
                      android:textColor="#FFAA00"
                      android:textSize="13sp"
                      android:gravity="center"
                      android:layout_marginBottom="12dp"/>

                  <Button
                      android:id="@+id/btn_accessibility"
                      android:layout_width="match_parent"
                      android:layout_height="wrap_content"
                      android:text="Open Accessibility Settings"
                      android:backgroundTint="#444444"/>

              </LinearLayout>
          </ScrollView>
          EOF

          cat > app/src/main/java/com/saned/autoswipe/MainActivity.java << 'EOF'
          package com.saned.autoswipe;

          import android.content.Intent;
          import android.content.SharedPreferences;
          import android.os.Bundle;
          import android.os.Handler;
          import android.os.Looper;
          import android.provider.Settings;
          import android.view.inputmethod.InputMethodManager;
          import android.widget.Button;
          import android.widget.EditText;
          import android.widget.Switch;
          import android.widget.TextView;
          import android.widget.Toast;
          import androidx.appcompat.app.AppCompatActivity;

          public class MainActivity extends AppCompatActivity {

              Switch switchService;
              EditText etPickupMax, etDropMax;
              TextView tvStatus, tvInfo;
              Button btnSave, btnAccessibility;
              SharedPreferences prefs;

              @Override
              protected void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);
                  setContentView(R.layout.activity_main);

                  prefs            = getSharedPreferences("saned_prefs", MODE_PRIVATE);
                  switchService    = findViewById(R.id.switch_service);
                  etPickupMax      = findViewById(R.id.et_pickup_max);
                  etDropMax        = findViewById(R.id.et_drop_max);
                  tvStatus         = findViewById(R.id.tv_status);
                  tvInfo           = findViewById(R.id.tv_info);
                  btnSave          = findViewById(R.id.btn_save);
                  btnAccessibility = findViewById(R.id.btn_accessibility);

                  etPickupMax.setText(String.valueOf(prefs.getFloat("pickup_max", 5.0f)));
                  etDropMax.setText(String.valueOf(prefs.getFloat("drop_max", 10.0f)));

                  switchService.setOnCheckedChangeListener((btn, isChecked) ->
                      updateStatus(isChecked)
                  );

                  btnSave.setOnClickListener(v -> {
                      float pMax, dMax;
                      try { pMax = Float.parseFloat(etPickupMax.getText().toString()); } catch (Exception e) { pMax = 5.0f; }
                      try { dMax = Float.parseFloat(etDropMax.getText().toString());   } catch (Exception e) { dMax = 10.0f; }

                      prefs.edit()
                          .putFloat("pickup_max", pMax)
                          .putFloat("drop_max", dMax)
                          .putBoolean("enabled", switchService.isChecked())
                          .apply();

                      // Keyboard aur cursor band karo
                      etPickupMax.clearFocus();
                      etDropMax.clearFocus();
                      InputMethodManager imm = (InputMethodManager) getSystemService(INPUT_METHOD_SERVICE);
                      if (imm != null && getCurrentFocus() != null)
                          imm.hideSoftInputFromWindow(getCurrentFocus().getWindowToken(), 0);

                      Toast.makeText(this, "Settings Saved!", Toast.LENGTH_SHORT).show();
                      tvStatus.setText("Settings Saved!");
                      tvStatus.setTextColor(0xFF1DB954);
                      new Handler(Looper.getMainLooper()).postDelayed(() ->
                          updateStatus(switchService.isChecked()), 1500);
                  });

                  btnAccessibility.setOnClickListener(v ->
                      startActivity(new Intent(Settings.ACTION_ACCESSIBILITY_SETTINGS))
                  );
              }

              private void updateStatus(boolean enabled) {
                  if (enabled) {
                      tvStatus.setText("Service ON - Waiting for order...");
                      tvStatus.setTextColor(0xFF1DB954);
                  } else {
                      tvStatus.setText("Service OFF");
                      tvStatus.setTextColor(0xFFFF5555);
                  }
              }

              @Override
              protected void onResume() {
                  super.onResume();
                  boolean enabled = prefs.getBoolean("enabled", false);
                  switchService.setChecked(enabled);
                  updateStatus(enabled);
                  if (isAccessibilityEnabled()) {
                      tvInfo.setText("Accessibility Service: Active");
                      tvInfo.setTextColor(0xFF1DB954);
                  } else {
                      tvInfo.setText("Pehle Accessibility Service enable karein!");
                      tvInfo.setTextColor(0xFFFFAA00);
                  }
              }

              private boolean isAccessibilityEnabled() {
                  try {
                      String s = Settings.Secure.getString(getContentResolver(),
                          Settings.Secure.ENABLED_ACCESSIBILITY_SERVICES);
                      return s != null && s.contains(getPackageName());
                  } catch (Exception e) { return false; }
              }
          }
          EOF

          cat > app/src/main/java/com/saned/autoswipe/AutoSwipeService.java << 'EOF'
          package com.saned.autoswipe;

          import android.accessibilityservice.AccessibilityService;
          import android.accessibilityservice.GestureDescription;
          import android.app.Notification;
          import android.app.NotificationChannel;
          import android.app.NotificationManager;
          import android.content.SharedPreferences;
          import android.graphics.Path;
          import android.graphics.Rect;
          import android.os.Build;
          import android.os.Handler;
          import android.os.Looper;
          import android.util.Log;
          import android.view.accessibility.AccessibilityEvent;
          import android.view.accessibility.AccessibilityNodeInfo;
          import java.util.List;

          public class AutoSwipeService extends AccessibilityService {

              private static final String TAG = "SanedAuto";
              private static final String CHANNEL_ID = "saned_auto_channel";
              private static final int NOTIF_ID = 1001;
              private SharedPreferences prefs;
              private final Handler handler = new Handler(Looper.getMainLooper());

              @Override
              public void onServiceConnected() {
                  super.onServiceConnected();
                  prefs = getSharedPreferences("saned_prefs", MODE_PRIVATE);
                  createNotificationChannel();
                  Log.d(TAG, "Service Connected");
              }

              private void createNotificationChannel() {
                  if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
                      NotificationChannel channel = new NotificationChannel(
                          CHANNEL_ID,
                          "Saned Auto Notifications",
                          NotificationManager.IMPORTANCE_HIGH
                      );
                      channel.setDescription("Order accept notifications");
                      channel.enableVibration(true);
                      NotificationManager nm = getSystemService(NotificationManager.class);
                      if (nm != null) nm.createNotificationChannel(channel);
                  }
              }

              private void sendOrderNotification(float pickupKm, float dropKm) {
                  try {
                      NotificationManager nm = (NotificationManager) getSystemService(NOTIFICATION_SERVICE);
                      if (nm == null) return;

                      Notification.Builder builder;
                      if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
                          builder = new Notification.Builder(this, CHANNEL_ID);
                      } else {
                          builder = new Notification.Builder(this);
                      }

                      builder.setSmallIcon(android.R.drawable.ic_dialog_info)
                          .setContentTitle("Order Accept Ho Gaya!")
                          .setContentText("Pickup: " + pickupKm + " km | Drop: " + dropKm + " km")
                          .setAutoCancel(true)
                          .setPriority(Notification.PRIORITY_HIGH);

                      nm.notify(NOTIF_ID, builder.build());
                      Log.d(TAG, "Notification sent!");
                  } catch (Exception e) {
                      Log.e(TAG, "Notification error: " + e.getMessage());
                  }
              }

              @Override
              public void onAccessibilityEvent(AccessibilityEvent event) {
                  if (prefs == null) return;
                  if (!prefs.getBoolean("enabled", false)) return;
                  String pkg = String.valueOf(event.getPackageName());
                  if (!pkg.contains("saned")) return;
                  handler.postDelayed(this::checkAndAccept, 400);
              }

              private void checkAndAccept() {
                  AccessibilityNodeInfo root = getRootInActiveWindow();
                  if (root == null) return;
                  try {
                      // "New Order" screen check
                      List<AccessibilityNodeInfo> newOrderList =
                          root.findAccessibilityNodeInfosByText("New Order");
                      if (newOrderList == null || newOrderList.isEmpty()) return;

                      float pickupMax = prefs.getFloat("pickup_max", 5.0f);
                      float dropMax   = prefs.getFloat("drop_max", 10.0f);

                      float pickupKm = findDistance(root, "From You");
                      float dropKm   = findDistance(root, "From Pickup");

                      Log.d(TAG, "Pickup: " + pickupKm + " | Drop: " + dropKm);
                      Log.d(TAG, "Max - Pickup: " + pickupMax + " | Drop: " + dropMax);

                      if (pickupKm < 0 || dropKm < 0) return;

                      boolean pickupOk = pickupKm <= pickupMax;
                      boolean dropOk   = dropKm   <= dropMax;

                      if (pickupOk && dropOk) {
                          // Accept button dhundo aur swipe karo
                          List<AccessibilityNodeInfo> acceptNodes =
                              root.findAccessibilityNodeInfosByText("Accept");
                          if (acceptNodes == null || acceptNodes.isEmpty()) {
                              acceptNodes = root.findAccessibilityNodeInfosByText("ACCEPT");
                          }
                          if (acceptNodes == null || acceptNodes.isEmpty()) {
                              acceptNodes = root.findAccessibilityNodeInfosByText("قبول");
                          }

                          if (acceptNodes != null && !acceptNodes.isEmpty()) {
                              AccessibilityNodeInfo btn = acceptNodes.get(0);
                              if (btn != null) {
                                  Rect bounds = new Rect();
                                  btn.getBoundsInScreen(bounds);

                                  // Button ke center se right tak swipe
                                  int startX = bounds.left + 80;
                                  int endX   = bounds.right - 20;
                                  int y      = bounds.centerY();

                                  final float pKm = pickupKm;
                                  final float dKm = dropKm;

                                  swipeOnScreen(startX, y, endX, y, () -> {
                                      Log.d(TAG, "ORDER ACCEPTED via SWIPE! Pickup:" + pKm + " Drop:" + dKm);
                                      prefs.edit().putBoolean("enabled", false).apply();
                                      sendOrderNotification(pKm, dKm);
                                  });

                                  btn.recycle();
                              }
                          }
                      } else {
                          Log.d(TAG, "IGNORED - pickup=" + pickupOk + " drop=" + dropOk);
                      }
                  } finally {
                      root.recycle();
                  }
              }

              private void swipeOnScreen(int x1, int y1, int x2, int y2, Runnable onDone) {
                  Path path = new Path();
                  path.moveTo(x1, y1);
                  path.lineTo(x2, y2);

                  GestureDescription.Builder builder = new GestureDescription.Builder();
                  builder.addStroke(new GestureDescription.StrokeDescription(path, 0, 300));

                  dispatchGesture(builder.build(), new GestureResultCallback() {
                      @Override
                      public void onCompleted(GestureDescription gestureDescription) {
                          handler.post(onDone);
                      }
                      @Override
                      public void onCancelled(GestureDescription gestureDescription) {
                          Log.e(TAG, "Swipe cancelled");
                      }
                  }, null);
              }

              private float findDistance(AccessibilityNodeInfo root, String keyword) {
                  List<AccessibilityNodeInfo> nodes =
                      root.findAccessibilityNodeInfosByText(keyword);
                  if (nodes == null || nodes.isEmpty()) return -1f;
                  for (AccessibilityNodeInfo node : nodes) {
                      String text = node.getText() != null ? node.getText().toString() : "";
                      node.recycle();
                      float km = parseKm(text);
                      if (km >= 0) return km;
                  }
                  return -1f;
              }

              private float parseKm(String text) {
                  try {
                      String[] parts = text.trim().split("\\s+");
                      if (parts.length > 0)
                          return Float.parseFloat(parts[0].replace(",", "."));
                  } catch (Exception e) {
                      Log.e(TAG, "Parse: " + e.getMessage());
                  }
                  return -1f;
              }

              @Override
              public void onInterrupt() {}
          }
          EOF

          ./gradlew assembleDebug --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Saned-Auto-Swipe-v5
          path: app/build/outputs/apk/debug/*.apk
